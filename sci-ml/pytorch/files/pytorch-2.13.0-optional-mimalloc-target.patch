--- a/c10/CMakeLists.txt
+++ b/c10/CMakeLists.txt
@@ -118,9 +118,12 @@ if(NOT BUILD_LIBTORCHLESS)
     target_compile_definitions(c10 PRIVATE SUPPORTS_BACKTRACE=0)
   endif()
 
-  if(USE_MIMALLOC)
+  if(USE_MIMALLOC AND TARGET mimalloc-static)
     target_link_libraries(c10 PRIVATE "mimalloc-static")
     add_dependencies(c10 mimalloc-static)
+  else()
+    # Gentoo can build with -mimalloc while PyTorch still defines USE_MIMALLOC.
+    target_compile_options(c10 PRIVATE "-UUSE_MIMALLOC")
   endif()
 
   if(LINUX)
